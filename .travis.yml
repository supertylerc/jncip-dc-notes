sudo: false
language: python
python:
  - "3.6"
cache: pip
script:
  - make publish
deploy:
  provider: pages
  target_branch: master
  email: code@tylerc.me
  name: Deployment Bot
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
